<h1>Edite o seu livro</h1>

<form class="form" action="/books/updatebook" method="POST">
  <input type="hidden" name="id" value="{{book.id}}">
  <div class="form-control">
    <label for="title">Titulo:</label>
    <input type="text" name="title" placeholder="Insira o nome do livro" value="{{book.title}}">
  </div>
  <div class="form-control">
    <label for="pageqtd">Numero de páginas:</label>
    <input type="number" name="pageqtd" placeholder="Insira a quantidade de páginas" value="{{book.pageqtd}}">
  </div>
  <input type="submit" value="Editar">
</form>